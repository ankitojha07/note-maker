<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Note Maker</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="w-full min-h-screen bg-zinc-900 flex flex-col p-5 gap-16 items-center"
    >
      <div class="md:w-6/12 w-11/12 border-2 border-zinc-800 rounded-md">
        <form
          action="/create"
          method="POST"
          class="flex flex-col gap-2 items-center p-4"
          onsubmit="return validateForm()"
        >
          <div class="titleInput w-full flex flex-col items-start gap-1">
            <input
              type="text"
              placeholder="Title goes here"
              class="block px-5 py-2 bg-zinc-800 w-full rounded-md text-white"
              name="title"
              id="title"
            />
            <p class="text-zinc-400 text-[8px] text-start">
              <span class="text-red-500">** Do not include fileType,</span> it
              will be text file (.txt) by default
            </p>
          </div>
          <textarea
            name="details"
            id="details"
            placeholder="Write your task here..."
            class="block px-5 py-2 bg-zinc-800 resize-none w-full rounded-md text-white"
          ></textarea>
          <input
            type="submit"
            value="Create Task"
            class="bg-blue-600 px-5 py-2 rounded-md text-white"
          />
        </form>
      </div>
      <div
        class="w-full flex md:flex-row flex-col flex-wrap gap-2 justify-center items-center"
      >
        <% if(files.length > 0) { %> <% files.forEach(function(val){ %>
        <div
          class="task text-white md:w-6/12 w-11/12 border-2 border-zinc-800 rounded-md p-6"
        >
          <h1 class="font-bold text-xl"><%= val %></h1>
          <div class="w-full flex justify-between items-center mt-4">
            <a href="/files/<%= val%>" class="text-blue-600">Read more.</a>
            <div class="text-sm flex gap-2">
              <a href="/delete/<%= val %>" class="text-red-500">Delete</a>
              <a href="/edit/<%= val %>" class="text-blue-500">Edit</a>
            </div>
          </div>
        </div>
        <% }); %> <% } else { %>
        <div class="no-tasks text-zinc-600">No tasks yet.</div>
        <% } %>
      </div>
    </div>
  </body>
  <script src="/js/test.js"></script>
</html>
