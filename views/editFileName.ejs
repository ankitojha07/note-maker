<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 flex text-white flex-col p-10">
      <a href="/" class="text-blue-600 inline-block mb-10">go back to home</a>
      <form action="/edit" method="POST">
        <input type="hidden" name="previousTitle" value="<%= file.title %>" />
        <label for="newTitle">New Title:</label>
        <input
          type="text"
          name="newTitle"
          id="newTitle"
          value="<%= file.title.replace('.txt', '') %>"
        />

        <label for="content">Content:</label>
        <textarea name="content" id="content"><%= file.content %></textarea>

        <button type="submit">Save Changes</button>
      </form>
    </div>
  </body>
  <script src="/js/test.js"></script>
</html>
